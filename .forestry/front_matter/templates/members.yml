---
label: Members
hide_body: true
fields:
- name: member
  type: field_group_list
  fields:
  - name: first_name
    type: text
    config:
      required: true
    label: First Name
  - name: role
    type: text
    config:
      required: false
    label: role
  - name: member_image
    type: file
    config:
      maxSize: 64
    label: member image
  config:
    min: 
    max: 
    labelField: 
  label: Members
