---
label: Members
hide_body: true
fields:
- name: member
  type: field_group_list
  fields:
  - name: first_name
    type: text
    config:
      required: true
    label: First Name
  config:
    min: 
    max: 
    labelField: 
  label: Members
- name: role
  type: text
  config:
    required: false
  label: role
- name: image_member
  type: file
  config:
    maxSize: 64
  label: image member
